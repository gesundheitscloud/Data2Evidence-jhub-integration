name: _test-http-duckdb (reusable)

on:
  workflow_call:
    inputs:
      attempt:
        required: true
        type: string
      runner:
        required: true
        type: string
      run_tag:
        required: true
        type: string
      changes_ui:
        required: true
        type: string
      reg_url:
        required: true
        type: string
      prepull_flow_images:
        required: true
        type: string

permissions:
  contents: read
  packages: write

env:
  REG_URL: ${{ inputs.reg_url }}
  PREPULL_FLOW_IMAGES: ${{ inputs.prepull_flow_images }}

jobs:
  test:
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 80
    env:
      VERSION: develop
      DOCKER_TAG_NAME: ${{ inputs.run_tag }}
      # The lean image, not the -devx one docker-compose-local.yml defaults to.
      # `-d ./plugins/functions` (used below) pulls in docker-compose-local.yml,
      # which pins d2e-trex:<tag>-devx. In that image alp-dataflow-gen-init fails
      # to boot ("failed to bootstrap runtime"), so *no* Prefect variables are
      # seeded and every flow run dies -- the cache flow crashes on import. The
      # devx tooling is unused here (docker-compose-ci.yml sets TREX_DX_ENABLED
      # false), so run the same lean image test_demosetup_dev does.
      TREX_IMAGE: ${{ inputs.reg_url }}/d2e-trex:${{ inputs.run_tag }}
      PLUGINS_IMAGE_TAG: ${{ inputs.run_tag }}
      PGUSER: postgres
      PGPASSWORD: mypass
      PGHOST: localhost
      PGPORT: 15432
      PGDATABASE: postgres
      TESTSCHEMA: demo_cdm
      isTestEnv: "true"
      isHttpTestRun: "true"
      HTTPTEST_DB_DIALECT: "duckdb"
    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Increase swapfile to 15G
        run: |
          sudo swapoff -a
          sudo fallocate -l 15G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          sudo swapon --show

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: npm install
        if: success() || failure()
        run: npm install

      - name: Download UI package tarball
        uses: actions/download-artifact@v4
        with:
          name: ui-tgz
          path: ./plugins/ui

      - name: Extract UI package
        run: |
          TGZ=$(ls ./plugins/ui/data2evidence-d2e-ui-*.tgz | head -1)
          rm -rf ./plugins/ui-built
          mkdir -p ./plugins/ui-built
          tar -xzf "$TGZ" -C ./plugins/ui-built --strip-components=1

      - name: Initialise d2e
        if: success() || failure()
        run: |
          init_choice=y ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v develop init
          grep LOGTO__D2E_APP__CLIENT_ID .env | sed -e "s/'//g" >> $GITHUB_ENV

      - name: Pre-pull flow images
        run: |
          for img in $PREPULL_FLOW_IMAGES; do
            docker pull "${REG_URL}/d2e/${img}:${DOCKER_TAG_NAME}"
          done

      - name: Pull docker compose images
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v develop -d ./plugins/functions pull
          docker images --format "table {{.ID}}\t{{.Repository}}\t{{.Tag}}"

      - name: Start services
        if: success() || failure()
        uses: nick-fields/retry@v3
        id: start
        with:
          timeout_seconds: 1800
          retry_wait_seconds: 100
          max_attempts: 2
          retry_on: any
          command: |
            ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }}  -d ./plugins/functions -c ./docker-compose-ci.yml start
            DC_EXIT_CODE=$?
            DOCKER_STATUS_FILE=../docker-status-${GITHUB_RUN_ID:-ghRunId}-$(date '+%Y%m%dT%H%M%S').txt | tee -a $GITHUB_ENV
            docker ps --format {{.Names}},{{.Status}} 2> /dev/null | sort | tee $DOCKER_STATUS_FILE | tee -a $GITHUB_STEP_SUMMARY
            [ $DC_EXIT_CODE = 0 ] || { echo EXIT DC_EXIT_CODE=$DC_EXIT_CODE && exit $DC_EXIT_CODE; }

      - name: Log status
        if: success() || failure()
        run: |
          echo DC_EXIT_CODE=$DC_EXIT_CODE | tee -a $GITHUB_STEP_SUMMARY
          echo RETRY_EXIT_CODE=${{ steps.start.outputs.exit_code }} | tee -a $GITHUB_STEP_SUMMARY
          echo RETRY_OUTCOME=${{ steps.start.outcome }} | tee -a $GITHUB_STEP_SUMMARY
          echo RETRY_TOTAL_ATTEMPTS=${{ steps.start.outputs.total_attempts }} | tee -a $GITHUB_STEP_SUMMARY
          DC_FAIL_COUNT=$(grep -icE "starting|unhealthy" $DOCKER_STATUS_FILE|| true)
          echo DC_FAIL_COUNT=$DC_FAIL_COUNT | tee -a $GITHUB_STEP_SUMMARY

      - name: Logs
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions logs

      - name: Wait for services to be ready before setup
        if: success() || failure()
        run: |
          for i in $(seq 1 60); do
            NOT_READY=$(docker ps --format '{{.Names}},{{.Status}}' | grep -E 'health: starting|unhealthy' || true)
            if [ -z "$NOT_READY" ]; then
              echo "All services healthy after ${i} checks"
              break
            fi
            echo "Waiting for services to become healthy (attempt ${i}/60)..."
            echo "$NOT_READY"
            sleep 5
          done
          docker ps --format '{{.Names}},{{.Status}}' | sort

      - name: Setup HTTP Test Env
        if: success() || failure()
        uses: nick-fields/retry@v3
        with:
          timeout_seconds: 1800
          retry_wait_seconds: 100
          max_attempts: 1
          retry_on: any
          command: |
            ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }}  -d ./plugins/functions setuphttptestenv
            DC_EXIT_CODE=$?
            DOCKER_STATUS_FILE=../docker-status-${GITHUB_RUN_ID:-ghRunId}-$(date '+%Y%m%dT%H%M%S').txt | tee -a $GITHUB_ENV
            docker ps --format {{.Names}},{{.Status}} 2> /dev/null | sort | tee $DOCKER_STATUS_FILE | tee -a $GITHUB_STEP_SUMMARY
            [ $DC_EXIT_CODE = 0 ] || { echo EXIT DC_EXIT_CODE=$DC_EXIT_CODE && exit $DC_EXIT_CODE; }

      - name: Get dataset Id
        run: |
          export resDataset=$(docker exec d2e-minerva-postgres-1 psql -h localhost -U postgres -p 5432 -d alp -c "select * from portal.dataset" 2>/dev/null)
          echo "$resDataset"
          export resDatasetDetail=$(docker exec d2e-minerva-postgres-1 psql -h localhost -U postgres -p 5432 -d alp -c "select * from portal.dataset_detail" 2>/dev/null)
          echo "$resDatasetDetail"
          export resTrexDB=$(docker exec d2e-minerva-postgres-1 psql -h localhost -U postgres -p 5432 -d alp -c "select * from trex.db" 2>/dev/null)
          echo "$resTrexDB"
          export DATASET_ID=$(docker exec d2e-minerva-postgres-1 psql -h localhost -U postgres -p 5432 -d alp -t -c "select id from portal.dataset where type IN ('omop', 'webapi')" 2>/dev/null | head -1 | xargs)
          echo "DATASET_ID=$DATASET_ID" >> $GITHUB_ENV

      - name: Logs
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions logs

      - name: Checkflow
        if: success() || failure()
        uses: nick-fields/retry@v3
        with:
          timeout_seconds: 1800
          retry_wait_seconds: 100
          max_attempts: 1
          retry_on: any
          command: |
            ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions checkflow
            DC_EXIT_CODE=$?
            [ $DC_EXIT_CODE = 0 ] || { echo EXIT DC_EXIT_CODE=$DC_EXIT_CODE && exit $DC_EXIT_CODE; }

      - name: Restart d2e services
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions -c ./docker-compose-ci.yml stop
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions -c ./docker-compose-ci.yml -p 41100 start

      - name: Wait for services to be ready to avoid mounting race condition
        if: inputs.changes_ui == 'true' || contains(github.base_ref, 'release/')
        run: sleep 10

      - name: Check status of docker containers
        run: docker ps -a

      # Endpoint or issuer drift breaks WebAPI at startup and Atlas3 at sign-in
      # with nothing in either log that names the cause, so the served document
      # is diffed against a checked-in copy instead. A failure here is a
      # deliberate decision to re-register relying parties: update
      # tests/golden/trex-oidc-discovery.json in the same commit that changes
      # the document, and read tests/golden/README.md first — it records what
      # the document claims that this deployment does not deliver.
      #
      # It also enforces the cutover's ordering constraint mechanically. The
      # trex that serves this document comes from ghcr.io/ohdsi/trexsql through
      # TREXSQL_REF (services/trex/Dockerfile.v2), not from this repository, so
      # a d2e change that lands ahead of its trex image fails here rather than
      # at a user's sign-in.
      - name: The OIDC discovery document has not drifted
        run: |
          set -euo pipefail
          # The port the restart above started the gateway on, and the one
          # tests/backend_integration_tests/lib/ci-login.sh expects.
          PORT=41100
          GOLDEN=tests/golden/trex-oidc-discovery.json
          test -f "$GOLDEN" || { echo "::error::$GOLDEN is missing"; exit 1; }

          # trex was restarted a step ago; poll rather than assume, so a slow
          # boot fails as a timeout that says so instead of as a diff against
          # an error page.
          CODE=000
          for i in $(seq 1 60); do
            CODE=$(curl -sk -o /tmp/discovery-raw.json -w '%{http_code}' --max-time 10 \
              "https://localhost:${PORT}/trex/oidc/.well-known/openid-configuration" || echo 000)
            echo "attempt ${i}/60: HTTP ${CODE}"
            if [ "$CODE" = "200" ]; then break; fi
            sleep 5
          done
          if [ "$CODE" != "200" ]; then
            echo "::error::the discovery endpoint never answered 200 (last ${CODE})"
            cat /tmp/discovery-raw.json || true
            exit 1
          fi

          # The gateway origin is the one thing that legitimately differs
          # between a local stack, CI and a real FQDN, so it is the one thing
          # normalised away. The /trex/oidc prefix is part of the contract and
          # is left alone.
          jq -S "walk(if type == \"string\" then sub(\"https://localhost:${PORT}\"; \"{{ISSUER_ORIGIN}}\") else . end)" \
            /tmp/discovery-raw.json > /tmp/discovery.json

          # A stale image is not drift, and reporting it as drift sends the
          # reader after the one thing that has not happened. The cutover moved
          # the four protocol endpoints under /oauth2/, so a document whose
          # authorization_endpoint is not there came from a trexsql image
          # predating it, and no edit to the golden file can fix that. Checked
          # before the diff so this diagnosis wins the ones it explains.
          SERVED_AUTHZ=$(jq -r '.authorization_endpoint // ""' /tmp/discovery.json)
          case "$SERVED_AUTHZ" in
            */oauth2/authorize) ;;
            *)
              echo "::error::this stack is serving a trexsql image from before the OIDC provider cutover - authorization_endpoint is '${SERVED_AUTHZ:-(absent)}', not '{{ISSUER_ORIGIN}}/trex/oidc/oauth2/authorize'. This is NOT discovery drift and the golden file is not what needs changing. Publish a trexsql image built from the trex branch and bump TREXSQL_REF in ALL THREE pins - services/trex/Dockerfile.v2, docker-compose-local.yml, and the devx build-args in .github/workflows/docker-build-push.yaml, which is the one this job's image is actually built from; see docs/trex-oidc-provider-cutover.md."
              exit 1
              ;;
          esac

          if ! diff -u "$GOLDEN" /tmp/discovery.json; then
            echo "::error::the OIDC discovery document has drifted from ${GOLDEN}"
            exit 1
          fi
          echo "discovery document matches ${GOLDEN}"

      - name: Log in and get Bearer token and sub
        run: |
          # One script for both IdPs; see the script for why the flows differ.
          # Exports BEARER_TOKEN, REFRESH_TOKEN and IDP_SUB to $GITHUB_ENV.
          bash tests/backend_integration_tests/lib/ci-login.sh
      - name: Check status of docker containers
        run: docker ps -a

      - name: Check PG DB connection
        run: |
          PGPASSWORD=$(echo $(docker exec d2e-trex printenv DEMO__DB_PASSWORD))
          sudo apt install -y postgresql-client
          pg_isready -d ${PGDATABASE} -h ${PGHOST} -p ${PGPORT} -U ${PGUSER}
          psql -h ${PGHOST} -p ${PGPORT} -U ${PGUSER} -d ${PGDATABASE}

      - name: Setup Test DB
        run: |
          PGPASSWORD=$(echo $(docker exec d2e-trex printenv DEMO__DB_PASSWORD))
          yarn --prefer-offline
          yarn inittestdb-pg

      - name: Install dependencies in backend integration tests
        run: |
          cd ./tests/backend_integration_tests/pg
          yarn

      - name: Set env vars for HTTP tests
        run: |
          echo "PGPASSWORD=$(echo $(docker exec d2e-trex printenv DEMO__DB_PASSWORD))" >> $GITHUB_ENV
          echo "PGPORT=${PGPORT}" >> $GITHUB_ENV
          echo "PGUSER=${PGUSER}" >> $GITHUB_ENV
          echo "PGHOST=${PGHOST}" >> $GITHUB_ENV
          echo "TESTSCHEMA=${TESTSCHEMA}" >> $GITHUB_ENV
          echo "HDIPORT=${PGPORT}" >> $GITHUB_ENV
          echo "HDIPW=$(echo $(docker exec d2e-trex printenv DEMO__DB_PASSWORD))" >> $GITHUB_ENV
          echo "HDIUSER=${PGUSER}" >> $GITHUB_ENV
          echo "HANASERVER=${PGHOST}" >> $GITHUB_ENV
          echo "PGDATABASE=${PGDATABASE}" >> $GITHUB_ENV
          echo "isHttpTestRun=${isHttpTestRun}" >> $GITHUB_ENV
          echo "isTestEnv=${isTestEnv:-false}" >> $GITHUB_ENV
          echo "HTTPTEST_DB_DIALECT=${HTTPTEST_DB_DIALECT:-duckdb}" >> $GITHUB_ENV

      - name: Run HTTP tests
        working-directory: ./tests/backend_integration_tests/pg
        run: |
          echo "HDIPORT: ${HDIPORT}"
          echo "HDIPW: ${HDIPW}"
          echo "HDIUSER: ${HDIUSER}"
          echo "HANASERVER: ${HANASERVER}"
          echo "PGDATABASE: ${PGDATABASE}"
          echo "isHttpTestRun: ${isHttpTestRun}"
          echo "isTestEnv: ${isTestEnv:-false}"
          echo "TESTSCHEMA: ${TESTSCHEMA}"
          echo "HTTPTEST_DB_DIALECT: ${HTTPTEST_DB_DIALECT}"
          yarn test-specs --test_schema_name=$TESTSCHEMA '--file_pattern=^(?!strategus_analysis_test).*_test\.js$'

      - name: Mark HTTP duckdb attempt success
        if: success()
        run: |
          mkdir -p ./_attempt
          touch ./_attempt/success

      - name: Upload HTTP duckdb attempt outcome
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: http-duckdb-attempt-${{ inputs.attempt }}
          path: ./_attempt/
          overwrite: true

      - name: Logs
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions logs

      - name: Stop d2e services
        if: success() || failure()
        run: |
          ENV_TYPE=remote CADDY__CONFIG=./deploy/caddy-config node ./scripts/dist/cli.js -e -v ${{ env.VERSION }} -d ./plugins/functions stop

      - name: Prune system
        if: always()
        run: docker system prune -af